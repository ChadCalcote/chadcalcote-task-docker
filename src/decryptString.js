import { argv } from "process";
import Encryption from './libs/Encryption'

const enc = Encryption('risk3sixty') // update to make dynamic
// const plainText = enc.decrypt(ciphertext); 

console.log("hello");